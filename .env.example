# قالب کلی — برای جزئیات ببین:
#   .env.local.example       → توسعه روی کامپیوتر (localhost:3000)
#   .env.production.example  → deploy روی VPS (digi-face.ir)

DATABASE_URL="postgresql://USERNAME:PASSWORD@HOST:5432/DATABASE?schema=public"
NEXTAUTH_SECRET="your-secret"
NEXTAUTH_URL="http://localhost:3000"
TEST_OTP_CODE=""
